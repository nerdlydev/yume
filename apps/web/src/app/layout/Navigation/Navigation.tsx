import { Dock } from './Dock';

/**
 * The Navigation component is the primary abstraction for global navigation.
 * It encapsulates the dock and any future responsive adaptations (e.g. sidebars).
 */
export function Navigation() {
  return <Dock />;
}
