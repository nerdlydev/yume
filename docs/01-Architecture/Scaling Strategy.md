# Scaling Strategy

We start with a Modular Monolith. 
We will only extract modules into microservices when scaling requires it. 
Technologies deferred until needed: Kubernetes, Kafka, Elasticsearch, GraphQL, gRPC.