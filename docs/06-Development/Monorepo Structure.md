# Monorepo Structure

## pnpm Workspaces & Turborepo

```text
aurora/
  apps/
    web, api, admin, docs
  packages/
    auth, database, validation, ui, types, logger, config, email, shared
  tooling/
    docker, scripts
```